{
  "qualifiedName": "server-cmd",
  "displayName": "CMD Server",
  "description": "MCP server for executing CMD and SSH commands.",
  "iconUrl": null,
  "remote": false,
  "deploymentUrl": null,
  "security": null,
  "tools": [],
  "connections": [
    {
      "type": "stdio",
      "stdioFunction": "config => ({ command: 'npx', args: ['-y', 'server-cmd'], env: { HOST: config.host, PORT: config.port.toString() } })",
      "configSchema": {
        "type": "object",
        "required": [],
        "properties": {
          "host": {
            "type": "string",
            "default": "localhost",
            "description": "The host address for the server."
          },
          "port": {
            "type": "number",
            "default": 8080,
            "description": "The port on which the server runs."
          }
        }
      }
    }
  ]
}