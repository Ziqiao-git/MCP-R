{
  "qualifiedName": "@sirmews/mcp-remote-server",
  "displayName": "Remote Server",
  "description": "Bridge between MCP clients and remote APIs.",
  "iconUrl": null,
  "remote": false,
  "deploymentUrl": null,
  "security": null,
  "tools": [],
  "connections": [
    {
      "type": "stdio",
      "stdioFunction": "config => ({ command: 'bun', args: ['run', 'index.ts'], env: { MCP_CONTROL_PLANE_URL: config.controlPlaneUrl } })",
      "configSchema": {
        "type": "object",
        "required": [
          "controlPlaneUrl"
        ],
        "properties": {
          "controlPlaneUrl": {
            "type": "string",
            "description": "URL to the remote configuration endpoint"
          }
        }
      }
    }
  ]
}